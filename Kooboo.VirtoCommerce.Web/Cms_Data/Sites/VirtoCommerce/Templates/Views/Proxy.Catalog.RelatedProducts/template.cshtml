@model VirtoCommerce.Foundation.Catalogs.Model.AssociationGroup

@foreach (var association in Model.Associations.OrderBy(a => a.Priority))
{
 @Html.ModuleHtml().Action("AssociatedItem", new { itemId = association.ItemId, name = "relateditem", associationType = association.AssociationType })
}                